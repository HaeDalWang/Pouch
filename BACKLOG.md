# 🗂️ pouch 백로그

> **ROADMAP은 확정된 순서, 백로그는 후보 풀이다.** 여기 항목이 채택되면 ROADMAP의
> Phase로 승격되고, 여기선 지워진다. 이 문서도 닳고 붙고 떨어진다.

## 채택 렌즈 — "pouch의 약속이 거짓말이 되는가"

모든 항목엔 **걸린 약속** 칸이 있다. 약속이 *깨지는* 것 = 필수, 약속이 *커지는* 것 = 있으면 좋겠다.

| 약속 | 뜻 |
| --- | --- |
| 🪨 맞춰짐 | 나에게 맞춰진다 |
| 🌊 진화 | 쓸수록 진화한다 (닳고·붙고·떨어진다, 떨어져도 개인화는 남는다) |
| 🦦 묶임 | 옆 사람과 묶일 수 있다 |
| 🔧 규칙은 코드로 | **pouch 자신의 내부 불변식**을 코드 제약으로 박는다 (예: vendored body 불변). 사용자가 에이전트에게 거는 규칙(boundary)은 이 원칙의 대상이 **아니다** — [D1] 참조 |

---

## 결정 (백로그가 아니라 이미 정한 것 — 흔들리지 않기 위해 기록)

### D1. boundary는 의도적으로 강제하지 않는다 (2026-07-05)

boundary는 주입(에이전트가 알고 존중하게) + 제안까지만. PreToolUse 하드 차단은 넣지 않는다.

- **층 구분**: "규칙은 코드로"는 pouch 내부 불변식용 원칙이다. boundary는 사용자가
  에이전트에게 거는 규칙 — 다른 층이고, 섞으면 안 된다.
- **거짓 안전감**: 진짜 강제가 필요한 prod 보호는 더 낮은 층의 일이다 — IAM, SCP, OPA.
  pouch가 앱 레이어에서 차단을 흉내 내면 "pouch가 막아주니까 IAM 느슨해도 되겠지"라는
  거짓 안전감을 만든다. 그게 차단 없음보다 위험하다.
- 누가 "boundary가 왜 안 막아?"라고 물으면: **막는 건 인프라 층 책임, pouch의 boundary는
  에이전트가 알고 존중하게 만드는 층**이라고 답한다.

### D2. 당분간 Claude Code 우선, 중립화는 마킹만 (2026-07-05)

에이전트 중립 추상화 레이어는 넣지 않는다(YAGNI). 대신 Claude-specific 결합 지점
(hook 연결, `~/.claude/plugins/` 경로 가정, settings.json 스키마 등)에 마커 주석을
남긴다 — 구조 슬롯이 아니라 **문서화된 부채 마커**라 지금 박아도 안 비싸고, 수요가
생겼을 때 "어디부터 손대지"를 안 헤맨다.

---

## 필수 — 약속이 깨지는 지점

> ~~upstream 증발 대비~~ — 2026-07-05 채택·완료 (ROADMAP Phase 4.6 후속 참조).
> ~~기억 위생~~ — 2026-07-05 정책 락·구현 완료 (ROADMAP Phase 4.6 ④ 참조).
> ~~백업/export~~ — 2026-07-07 v0 완료 (`pouch backup`/`restore`, 로컬 어댑터. ROADMAP Phase 4.6 후속 참조).
> ~~usage.jsonl 위생~~ — 2026-07-07 완료 (접기/compaction, evolve 때 자동. ROADMAP Phase 4.6 후속 참조).
> ~~커버리지 갭 (필수 3) + P1(boundary 출처)~~ — 2026-07-07 완료. command·agent·rule import +
> boundary direction/source + 승격 통로 + drop gate. **훅도 같은 날 완료** — 6종 전부 열림.
> (조리법으로 담고 settings.json 배선, 설치 때 명령 원문 표시+동의, 신호 없는 종류는
> drop 후보 제외 + `catalog uninstall` 신설.) ROADMAP Phase 4.6 후속 참조.
> 시작 세트에 담을 재료가 다 갖춰졌다 — system prompt만 신규 kind 논의로 남음.
> ~~시작 세트 (필수 4)~~ — 2026-07-07 배관 완료(세트 형식·apply·init 제안·`pouch set`).
> 단 **내장 세트 내용물은 같은 날 철회** — 임의 큐레이션("DevOps면 이거 쓰겠지")은 실사용과
> 달랐다(배승도 검증). 지어낸 세트 = "남이 만든 정답 세트". 내장엔 실사용에서 굳힌 것만
> 들어온다(`sets/builtin/README.md` 원칙) → 굳히는 문은 아래 "세트 내보내기" 참조.
> (b) 공유·스타·편입 순환은 Phase 5 raft에 예약된 대로. system prompt kind는 여전히 논의 대상.

### ~~먼저 내미는 제안 (2026-07-09 승격)~~ — ✅ 완료 (2026-07-09) — 🪨🌊

**걸린 약속:** 🪨 맞춰짐이 첫날 이후 거짓말이 된다. "새 도구 더하기"도 "쓸수록
진화(evolve)"도 사용자가 먼저 찾아가야 일어나는데, 명령어를 모르는 사람에겐 🪨가
첫날부터 공회전 → 깨지는 지점이라 필수였다. 정책 근거·세부는
[[pouch-proactive-nudge-policy]].

**6조각 전부 구현·배선 완료 (같은 날 승격→완료).** 새 시스템 안 지음 — 이미 가진
3개(SessionStart 주입 통로·usage.jsonl·설치 동의 흐름) 재사용. 기본은 침묵,
무시엔 물러남(절대 조르지 않음)을 코드+테스트로 박음:

1. ~~제안 장부~~ — `~/.pouch/proposals.json` 사이드카(`evolution/ledger.py`).
   proposal_id → last_shown_at·shown_count만. 정밀화 반영: 묵히기는 반자동이라
   명시 스누즈 저장 안 함(심은 사실만).
2. ~~결과 예고~~ — `ChangePreview`(`evolution/preview.py`). 항목마다 효과+되돌리는
   정확한 한 줄. **정직성 불변식**: undo는 실재 CLI만, adopt·observe는 None(없는
   명령 지어내면 그 지어내기를 pouch에 박는 꼴). drop·reattach 성공 보고를 이
   단일 출처에서 뽑도록 배선(산문 흩어짐 제거).
   - ~~하드 선행: 주입 통로 구역 분리~~ — `render_session_context`
     (`memory/context.py`). 고정 구역(반드시 읽음) 위 / 쪽지 구역(무시 가능) 아래.
     한 구현으로 불변식 ①+③ 동시 만족(고정 먼저 렌더=③b 비대칭, 쪽지는 격리
     try=③a·①), 빈 내용이면 구분선조차 없음(②). churn 분리의 네 번째 적용.
3. ~~쪽지 렌더링~~ — `render_note`(`evolution/nudge.py`). 당기는 말('정리하자')로
   명령어 입구 안내, 크기 힌트만(말만 심기), '보여드릴게요'로 두 단계 예고.
4. ~~무시엔 물러남 + 라이브 배선~~ — `backoff_days`(지수+상한, 단조 비감소=키우기
   금지) + `plan_nudge`(방어 4개 집결) + `session_nudge.py`(조립: 장부 읽어 물러남
   판정, 심을 때만 record_shown=유령 기록 방지). `memory context`가 격리 note_zone에
   얹음(단방향 의존).
5. ~~adopt를 통로에~~ — 별도 코드 없음. 4b `gather_nudge_summary`가 adopt를 세며
   흡수(이미 있는 걸 통로에 실음).
6. ~~두 단계 동의 다리~~ — `pouch evolve --dry-run`(에이전트 중개, 배승도 선택 B).
   읽기전용 목록·이유·되돌림 예고(볼게), 사용자 '해'면 에이전트가 `--yes`로 재호출.
   `_preview_plan`이 조각2 preview의 effect+undo를 뱉어 지어내기 봉쇄. CLI 대체가
   아니라 CLI로 데려다줌.

### ~~이거 써봐 (2026-07-12 승격)~~ — ✅ 완료 (2026-07-12) — 🪨

**걸린 약속:** 위 "먼저 내미는 제안"은 발견 4단계 중 3(이해)·4(기억)만 열고
**1(있는 줄 알기)은 사정권 밖**으로 뒀다. 명령어도 모르고 **뭐가 있는지도 모르는**
사람에겐 🪨 맞춰짐이 첫날부터 거짓말 — 1번을 여는 게 이 항목. nudge의 "정찰 defer"를
부분적으로 여는데, **추측이 아니라 반복 위에서만** 열어 잔소리 함정을 피한다. 정책·근거
[[pouch-try-this-recommend-policy]].

**3조각 전부 완료 (같은 날 승격→완료).** 철회된 런치템플릿과 다름: 후보 리스트를
보여주고 **사용자가 고른다**(안 넣고 안 정함). 살아남는 교훈은 하나 — 설명·태그를
지어내지 않고 도구 자기 파일(SKILL.md)에서만:

1. ~~풀 만들기~~ — `evolution/pool.py`(build_pool). 카탈로그를 {id·설명·태그} 뷰로.
   태그 두 곳(ToolEntry.tags+Overlay.tags) 병합. 읽기만·지어내기 0.
2. ~~비슷한 것 찾기~~ — `evolution/similar.py`(find_similar). 태그 겹침으로만 판정
   (지어내기 없음), shared_tags로 왜 비슷한지 실재 근거. 날것 예외(태그 없으면 []).
3. ~~기존 통로에 얹기~~ — `plan_try_this`+`_render_try_this`. evolve dry-run·라이브
   두 경로에 배선. 앵커=반복 신호(reattach·adopt) 재사용, active·앵커 제외.
   새 파이프 없음. adopt 앵커(풀 밖)는 날것 예외로 조용히 빠짐(하드코딩 없이).

**남은 것(다음 조각):** (a) 질문·작업 반복 신호원 — 지금 usage.jsonl엔 도구 호출만
찍힘, "반복 질문"은 새 신호원 필요(민감 → 날것 저장 대신 주제·꼬리표만). (b) 바깥
마켓 풀 — v0는 이미 깔린 것(카탈로그)만. "남들이 많이 쓴다" 인기 데이터는 raft(Phase
5)가 진짜로 만들 때까지 없음(지어내면 런치템플릿 철회 재발).

---

## 있으면 좋겠다 — 약속이 커지는 것

> ~~세트 내보내기~~ — 2026-07-16 완료(`pouch set export`, PR #8). "수 주 실사용 후 착수"
> 예정보다 앞당겨졌다 — 단 내장 세트 승격 심사는 여전히 사람 몫(명령은 문만 연다).
> ~~프로젝트별 주머니~~ — 2026-07-15 완료(프로젝트 스코프 카탈로그, docs/PROJECT-POUCH.md).
> ~~주간 리포트~~ — 2026-07-15 완료(`pouch report`, TOOLKIT-OPTIMIZATION 후보 A).
> ~~owned 임베드~~ — 백로그를 거치지 않고 2026-07-17 오너 실수요로 ROADMAP에 직행
> (직접 만든 도구를 세트에 품기 — 다음 기능 1순위, Phase 5 후속 참조).

| 항목 | 걸린 약속 | 메모 |
| --- | --- | --- |
| 시맨틱 recall | 🪨 | 지금은 키워드 매칭 — 기억 100개 넘으면 회상 품질이 병목. MEMORY-POSITIONING 열린 결정 #3(회상 백엔드 추상화)과 한 몸 |
| init 낱개 도구 추천 배선 | 🪨 | recommend() 계산은 있는데 init이 안 부름(HOW-IT-WORKS의 "솔직한 구멍"). 콜드 스타트 답은 세트라 급하지 않음 |
| doctor 완전판 | 🪨 가시성 | 무엇이 연결됐고 어떤 효과인지 사람 말로 풍부하게. 축소판은 Phase 4.6 ②로 이미 승격됨 |
| Claude 결합 지점 마킹 | (D2의 실행 항목) | 싸고 작음 — 다른 작업 하는 김에 점진적으로 |
| system prompt kind | 🪨 | 담는 종류 7번째 후보 — 논의만 예약(필수 3 완료 때 남긴 것) |

---

## 예고된 문제 — 지금 안 하지만, 터질 자리를 알고 있는 것

### P1. boundary의 출처(provenance) — ✅ 해소 (2026-07-07)

필수 3과 함께 정면으로 설계·완료했다. 확정 그림:

- **direction 필드** (allow/ask/deny) — boundary 설계가 "deny 오독 터질 때 박는다"고
  예고한 그 필드를, drop을 방향으로 가르는 요구가 정당하게 당겼다. 산문에서 방향을
  뽑지 않고(오독 위험) 명시 라벨로만 읽는다. context 주입에 `[DENY]` 라벨로 강조.
- **source 필드** (`user` / `vendored:<도구id>`) — 사람이 건 것과 도구가 딸고 온 것을
  가른다. CLI로는 `user`만 가능(참칭 불가), `vendored:`는 승격 통로만 프로그램으로 새김.
- **승격 통로** — 엔트리의 권장 boundary(`RecommendedBoundary` 씨앗)가 설치 시 진짜
  boundary 메모리로 태어나며 출처 도장을 단다. 없을 때만 심음(재부착이 사용자 편집 안 뭉갬).
- **drop gate** — 도구 내릴 때 그 도구 출신 중 **allow는 함께 강등, ask·deny·방향불명은
  잔존+경고**. 사람이 건 것은 방향 무관 잔존. 안전 비대칭(허용 좁게·금지 넓게)과 일치.

근거·세부는 [[pouch-boundary-design]] 메모리, 용어는 [GLOSSARY.md](docs/GLOSSARY.md)
(경계의 방향/출처) 참조.

---

## ROADMAP에 이미 있는 것 (중복 기재 안 함)

- 기억 위생 + 저마찰 축적 — Phase 4.6 ④ (완료, 2026-07-05)
- 태그 승격 — Phase 4.5
- 주머니 공유(raft) — Phase 5
